import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    white: 'white',
    ourgreen: '#7FD661',
    primary: 'black',
    secondary: '#80D2F1',
    error: '#f13a59',
    background: '#CFECC5',
  },
};

export default theme;
