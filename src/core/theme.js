import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    white: 'white',
    primary: '#80D2F1',
    secondary: '#7FD661',
    error: '#f13a59',
    background: '#CFECC5',
  },
};

export default theme;
